const { ethers } = require("hardhat");

const STARGATE_POOL_USDT = "";

async function main() {}

main()
  .then(() => process.exit(0))
  .catch((error) => {
    console.error(error);
    process.exit(1);
  });
